export interface Post {
  postTitle: string
  postBody: string
  postImages: string[]
}

export interface user {
  firstName: string
  secondName: string
  uid: string
  dateOfBirth: number
  biography?: string
}